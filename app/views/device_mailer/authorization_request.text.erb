======================================
🔐 새 디바이스 승인 요청
======================================

안녕하세요, <%= @user.name || @user.email %>님

새로운 디바이스에서 계정 로그인 시도가 감지되었습니다.

디바이스 정보
--------------
디바이스: <%= @device.device_name %>
브라우저: <%= @device.browser %>
운영체제: <%= @device.os %>
시도 시간: <%= l(@device.created_at, format: :long) %>

본인의 로그인 시도가 맞다면 아래 링크를 클릭하여 디바이스를 승인해주세요:

<%= @approval_url %>

⚠️ 주의사항
--------------
* 이 링크는 24시간 동안 유효합니다.
* 본인의 시도가 아니라면 이 이메일을 무시하고 비밀번호를 변경하세요.
* 링크를 다른 사람과 공유하지 마세요.

======================================
생산관리시스템
이 이메일은 자동으로 발송되었습니다.
======================================
