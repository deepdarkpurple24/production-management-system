<%= render 'recipes/shared/sub_navigation' %>

<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <div>
      <h4 class="mb-0" style="font-weight: 600; letter-spacing: -0.02em;">완제품 등록</h4>
      <p class="text-muted small mb-0">New Finished Product</p>
    </div>
  </div>

  <div class="card-modern">
    <div class="card-body">
      <%= render 'form', finished_product: @finished_product %>
    </div>
  </div>
</div>
