<%= render 'inventory/shared/sub_navigation' %>

<div class="container mt-4">
  <!-- 헤더 섹션 -->
  <div class="mb-4">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><%= link_to "재고관리", inventory_path %></li>
        <li class="breadcrumb-item"><%= link_to "출고관리", inventory_shipments_path %></li>
        <li class="breadcrumb-item active" aria-current="page">출고 수정</li>
      </ol>
    </nav>
    <h2 class="mb-1" style="font-weight: 600; letter-spacing: -0.02em;">출고 수정</h2>
    <p class="text-muted small mb-0">Edit Shipment</p>
  </div>

  <!-- 수정 폼 -->
  <div class="card-apple">
    <div class="card-body p-4">
      <%= render 'form', shipment: @shipment %>
    </div>
  </div>
</div>
