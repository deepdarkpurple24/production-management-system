<div class="container-apple py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0">내 디바이스 관리</h1>
  </div>

  <!-- My Devices -->
  <div class="card-apple mb-4">
    <div class="card-apple-header">
      <h5 class="mb-0">
        <i class="bi bi-laptop me-2"></i>
        승인된 디바이스
      </h5>
    </div>
    <div class="card-apple-body">
      <% if @devices.empty? %>
        <p class="text-muted text-center py-4 mb-0">
          승인된 디바이스가 없습니다.
        </p>
      <% else %>
        <div class="table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>디바이스</th>
                <th>브라우저/OS</th>
                <th>상태</th>
                <th>마지막 사용</th>
                <th class="text-end">작업</th>
              </tr>
            </thead>
            <tbody>
              <% @devices.each do |device| %>
                <tr id="device-<%= device.id %>">
                  <td>
                    <div class="d-flex align-items-center">
                      <i class="bi bi-<%= device.active ? 'check-circle-fill text-success' : 'x-circle-fill text-muted' %> me-2"></i>
                      <div>
                        <strong><%= device.display_name %></strong>
                        <% if device.pending? %>
                          <span class="badge bg-warning ms-2">승인 대기</span>
                        <% elsif device.rejected? %>
                          <span class="badge bg-danger ms-2">거부됨</span>
                        <% end %>
                      </div>
                    </div>
                  </td>
                  <td>
                    <div class="text-muted" style="font-size: 0.875rem;">
                      <%= device.browser %> / <%= device.os %>
                    </div>
                  </td>
                  <td>
                    <% if device.active %>
                      <span class="badge bg-success">활성</span>
                    <% else %>
                      <span class="badge bg-secondary">비활성</span>
                    <% end %>
                  </td>
                  <td>
                    <% if device.last_used_at %>
                      <span class="text-muted" style="font-size: 0.875rem;">
                        <%= time_ago_in_words(device.last_used_at) %> 전
                      </span>
                    <% else %>
                      <span class="text-muted" style="font-size: 0.875rem;">-</span>
                    <% end %>
                  </td>
                  <td class="text-end">
                    <!-- Rename Button -->
                    <button type="button" class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#renameModal-<%= device.id %>">
                      <i class="bi bi-pencil"></i>
                    </button>
                    <!-- Delete Button -->
                    <%= button_to my_device_path(device, current_fingerprint: params[:fingerprint]), method: :delete, class: "btn btn-sm btn-outline-danger", data: { confirm: "이 디바이스를 삭제하시겠습니까?" } do %>
                      <i class="bi bi-trash"></i>
                    <% end %>

                    <!-- Rename Modal -->
                    <div class="modal fade" id="renameModal-<%= device.id %>" tabindex="-1">
                      <div class="modal-dialog">
                        <div class="modal-content">
                          <%= form_with model: device, url: my_device_path(device), method: :patch do |f| %>
                            <div class="modal-header">
                              <h5 class="modal-title">디바이스 이름 변경</h5>
                              <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                            </div>
                            <div class="modal-body">
                              <%= f.text_field :device_name, class: "form-control", placeholder: "예: 집 컴퓨터, 회사 노트북" %>
                            </div>
                            <div class="modal-footer">
                              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">취소</button>
                              <%= f.submit "변경", class: "btn-apple btn-apple-primary" %>
                            </div>
                          <% end %>
                        </div>
                      </div>
                    </div>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Login History -->
  <div class="card-apple">
    <div class="card-apple-header">
      <h5 class="mb-0">
        <i class="bi bi-clock-history me-2"></i>
        최근 로그인 이력
      </h5>
    </div>
    <div class="card-apple-body">
      <% if @login_histories.empty? %>
        <p class="text-muted text-center py-4 mb-0">
          로그인 이력이 없습니다.
        </p>
      <% else %>
        <div class="table-responsive">
          <table class="table table-sm">
            <thead>
              <tr>
                <th>시간</th>
                <th>디바이스</th>
                <th>IP 주소</th>
                <th>상태</th>
              </tr>
            </thead>
            <tbody>
              <% @login_histories.each do |history| %>
                <tr>
                  <td>
                    <span class="text-muted" style="font-size: 0.875rem;">
                      <%= l(history.attempted_at, format: :short) %>
                    </span>
                  </td>
                  <td>
                    <span style="font-size: 0.875rem;"><%= history.device_name %></span>
                  </td>
                  <td>
                    <span class="text-muted" style="font-size: 0.875rem;"><%= history.ip_address %></span>
                  </td>
                  <td>
                    <%= content_tag :span, history.display_status, class: "badge bg-#{history.status_color}" %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% end %>
    </div>
  </div>
</div>
