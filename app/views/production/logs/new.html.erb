<div class="container-fluid px-4 py-4">
  <!-- 날짜 네비게이션 -->
  <div class="card-modern mb-3">
    <div class="card-body py-2 px-3">
      <div class="d-flex justify-content-between align-items-center">
        <%= link_to new_production_log_path(date: @production_log.production_date - 1.day),
                    class: "btn btn-sm btn-outline-secondary" do %>
          <i class="bi bi-chevron-left"></i>
        <% end %>

        <div class="text-center">
          <h5 class="mb-0" style="font-weight: 600;">
            <%= @production_log.production_date.strftime('%Y년 %m월 %d일 (%a)') %>
          </h5>
          <%= link_to "오늘", new_production_log_path(date: Date.today),
                      class: "btn btn-sm btn-link text-muted" %>
        </div>

        <%= link_to new_production_log_path(date: @production_log.production_date + 1.day),
                    class: "btn btn-sm btn-outline-secondary" do %>
          <i class="bi bi-chevron-right"></i>
        <% end %>
      </div>
    </div>
  </div>

  <% if @production_plans.any? %>
    <!-- 생산 계획 탭 -->
    <div class="card-modern">
      <div class="card-body p-0">
        <!-- 탭 네비게이션 -->
        <ul class="nav nav-tabs border-bottom-0" id="productionTabs" role="tablist" style="padding: 1rem 1rem 0 1rem;">
          <% @production_plans.each_with_index do |plan, index| %>
            <li class="nav-item" role="presentation">
              <button class="nav-tab-modern <%= 'active' if index == 0 %>"
                      id="plan-<%= plan.id %>-tab"
                      data-bs-toggle="tab"
                      data-bs-target="#plan-<%= plan.id %>"
                      type="button"
                      role="tab">
                <div class="d-flex align-items-center gap-2">
                  <strong><%= plan.finished_product.name %></strong>
                  <span class="badge bg-secondary" style="font-size: 0.7rem;">
                    <%= number_with_delimiter(plan.quantity) %><%= plan.finished_product.name == '기정떡' ? '통' : '개' %>
                  </span>
                  <% if plan.production_logs.any? %>
                    <i class="bi bi-check-circle-fill text-success" style="font-size: 0.9rem;"></i>
                  <% end %>
                </div>
              </button>
            </li>
          <% end %>
        </ul>

        <!-- 탭 내용 -->
        <div class="tab-content" id="productionTabsContent">
          <% @production_plans.each_with_index do |plan, index| %>
            <div class="tab-pane fade <%= 'show active' if index == 0 %>"
                 id="plan-<%= plan.id %>"
                 role="tabpanel">
              <div class="p-4">
                <%
                  # 완제품의 레시피 목록 가져오기
                  recipes = plan.finished_product.recipes

                  # 레시피가 없으면 완제품 자체에 대한 일지 작성
                  if recipes.empty?
                    recipes = [nil]  # nil로 표시하여 완제품에 대한 기본 폼 생성
                  end
                %>

                <% recipes.each do |recipe| %>
                  <% if recipes.count > 1 %>
                    <!-- 여러 레시피인 경우 레시피명 표시 -->
                    <div class="alert alert-info mb-3" style="background-color: #e3f2fd; border: none;">
                      <h6 class="mb-0" style="color: #1976d2; font-weight: 600;">
                        <i class="bi bi-journal-text me-2"></i><%= recipe&.name || plan.finished_product.name %> 반죽일지
                      </h6>
                    </div>
                  <% end %>

                  <%
                    # 이미 등록된 일지가 있는지 확인 (recipe_id로 구분)
                    existing_log = plan.production_logs.find_by(recipe_id: recipe&.id)
                    log = existing_log || ProductionLog.new(
                      production_plan: plan,
                      finished_product: plan.finished_product,
                      recipe: recipe,
                      production_date: plan.production_date
                    )
                    form_url = existing_log ? production_log_path(existing_log) : production_logs_path
                    form_method = existing_log ? :patch : :post
                  %>

                <!-- 반죽일지 폼 -->
                <%= form_with(model: log, url: form_url, method: form_method, local: true) do |form| %>
                  <%= form.hidden_field :production_plan_id, value: plan.id %>
                  <%= form.hidden_field :finished_product_id, value: plan.finished_product_id %>
                  <%= form.hidden_field :recipe_id, value: recipe&.id %>

                  <% if log.errors.any? %>
                    <div class="alert alert-danger mb-4" role="alert">
                      <h5 class="alert-heading mb-3">
                        <i class="bi bi-exclamation-triangle me-2"></i>
                        <%= pluralize(log.errors.count, "error") %> prohibited this production log from being saved:
                      </h5>
                      <ul class="mb-0">
                        <% log.errors.each do |error| %>
                          <li><%= error.full_message %></li>
                        <% end %>
                      </ul>
                    </div>
                  <% end %>

                  <!-- 생산 일시 -->
                  <div class="row mb-3">
                    <div class="col-md-6">
                      <%= form.label :production_date, "생산 날짜", class: "form-label", style: "font-weight: 500;" %>
                      <span class="text-danger">*</span>
                      <%= form.date_field :production_date,
                                          class: "form-control",
                                          required: true %>
                      <small class="form-text text-muted">생산 날짜를 입력하세요.</small>
                    </div>
                    <div class="col-md-6">
                      <%= form.label :production_time, "생산 시간", class: "form-label", style: "font-weight: 500;" %>
                      <%= form.time_field :production_time,
                                          value: log.production_time || Time.current.strftime('%H:%M'),
                                          class: "form-control",
                                          placeholder: "예: 09:00" %>
                      <small class="form-text text-muted">생산 시작 시간을 입력하세요.</small>
                    </div>
                  </div>

                  <!-- 기정떡 전용 필드 섹션 -->
                  <div id="gijeongddeok-fields-<%= plan.id %>" style="<%= plan.finished_product.name.include?('기정떡') ? '' : 'display: none;' %>">
                    <hr class="my-4">
                    <h5 class="mb-3" style="color: #1d1d1f; font-weight: 600;">
                      <i class="bi bi-clipboard-data me-2"></i>기정떡 생산 상세 정보
                    </h5>

                    <!-- 설정된 순서대로 필드 렌더링 -->
                    <div class="card mb-3">
                      <div class="card-header" style="background-color: #f5f5f7;">
                        <h6 class="mb-0" style="font-weight: 500;">📝 생산 정보</h6>
                      </div>
                      <div class="card-body">
                        <div class="row g-3">
                          <% @gijeongddeok_fields.each do |field| %>
                            <% next if field.field_name == 'makgeolli_consumption' %>
                            <div class="col-md-4">
                              <%= form.label field.field_name, field.label, class: "form-label" %>
                              <div class="input-group">
                                <% if field.field_name == 'dough_count' %>
                                  <button type="button" class="btn btn-outline-secondary" onclick="adjustValue('production_log_<%= plan.id %>_<%= field.field_name %>', -0.5)">
                                    <i class="bi bi-dash"></i>
                                  </button>
                                <% end %>
                                <%= form.number_field field.field_name,
                                                      step: field.field_name == 'dough_count' ? 0.5 : ((field.field_name.ends_with?('_temp') || field.field_name.ends_with?('_amount') && field.field_name != 'yeast_amount' && field.field_name != 'salt_amount' && field.field_name != 'sugar_amount') ? 0.1 : nil),
                                                      value: field.field_name == 'dough_count' ? (log.dough_count || plan.quantity) : (log.persisted? ? log.send(field.field_name) : @gijeongddeok_default.send(field.field_name)),
                                                      class: "form-control",
                                                      id: "production_log_#{plan.id}_#{field.field_name}",
                                                      placeholder: field.category == 'temperature' ? "예: 25.0" : (field.field_name == 'water_amount' ? "예: 1.5" : "예: 100") %>
                                <% if field.field_name == 'dough_count' %>
                                  <button type="button" class="btn btn-outline-secondary" onclick="adjustValue('production_log_<%= plan.id %>_<%= field.field_name %>', 0.5)">
                                    <i class="bi bi-plus"></i>
                                  </button>
                                  <span class="input-group-text">통</span>
                                <% else %>
                                  <span class="input-group-text">
                                    <%= case field.field_name
                                        when /temp$/
                                          '°C'
                                        when 'yeast_amount', 'salt_amount', 'sugar_amount', 'steiva_amount'
                                          'g'
                                        when 'water_amount'
                                          'L'
                                        else
                                          ''
                                        end %>
                                  </span>
                                <% end %>
                              </div>
                            </div>
                          <% end %>
                        </div>
                      </div>
                    </div>

                    <!-- 막걸리 관리 -->
                    <div class="card mb-3">
                      <div class="card-header" style="background-color: #f5f5f7;">
                        <h6 class="mb-0" style="font-weight: 500;">🍶 막걸리 관리</h6>
                      </div>
                      <div class="card-body">
                        <div class="row g-3">
                          <div class="col-md-6">
                            <%= form.label :makgeolli_consumption, "막걸리 소비량", class: "form-label" %>
                            <div class="input-group">
                              <%= form.number_field :makgeolli_consumption,
                                                    step: 0.1,
                                                    value: log.persisted? ? log.makgeolli_consumption : @gijeongddeok_default.makgeolli_consumption,
                                                    class: "form-control",
                                                    id: "production_log_#{plan.id}_makgeolli_consumption",
                                                    placeholder: "예: 2500" %>
                              <span class="input-group-text">mL</span>
                            </div>
                            <small class="form-text text-muted">입력 시 자동으로 재고 출고 처리됩니다.</small>
                          </div>

                          <div class="col-md-6">
                            <%= form.label :makgeolli_expiry_date, "막걸리 유효기간", class: "form-label" %>
                            <%= form.date_field :makgeolli_expiry_date,
                                                class: "form-control" %>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>

                  <!-- 기본 비고 -->
                  <div class="mb-3">
                    <%= form.label :notes, "비고", class: "form-label", style: "font-weight: 500;" %>
                    <%= form.text_area :notes,
                                       class: "form-control",
                                       rows: 3,
                                       placeholder: "생산 과정 중 특이사항이나 메모를 입력하세요 (선택사항)" %>
                  </div>

                  <div class="d-flex justify-content-end gap-2 mt-4">
                    <%= form.submit existing_log ? "수정" : "등록",
                                    class: "btn-modern btn-modern-primary" %>
                    <%= link_to "취소", production_logs_path,
                                class: "btn-modern btn-modern-outline" %>
                  </div>
                <% end %>

                <% if recipes.count > 1 && recipe != recipes.last %>
                  <!-- 레시피 간 구분선 -->
                  <hr class="my-4" style="border-top: 2px dashed #d2d2d7;">
                <% end %>
              <% end %> <!-- recipes.each 종료 -->
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  <% else %>
    <!-- 생산 계획 없음 -->
    <div class="card-modern">
      <div class="card-body text-center py-5">
        <i class="bi bi-calendar-x" style="font-size: 3rem; color: #d2d2d7;"></i>
        <p class="text-muted mt-3 mb-0">오늘(<%= Date.today.strftime('%Y년 %m월 %d일') %>)에 등록된 생산 계획이 없습니다.</p>
        <%= link_to production_plans_path(date: Date.today),
                    class: "btn-modern btn-modern-primary mt-3" do %>
          <i class="bi bi-calendar-plus me-2"></i>생산 계획 등록
        <% end %>
      </div>
    </div>
  <% end %>
</div>

<script>
  // 기본값 로드 함수
  function loadGijeongddeokDefaults() {
    // 기정떡 기본값 데이터
    const defaults = {
      <% @gijeongddeok_fields.each do |field| %>
        <%= field.field_name %>: <%= @gijeongddeok_default.send(field.field_name).to_f %>,
      <% end %>
    };

    // 각 탭의 폼을 확인하고 기본값 적용
    <% @production_plans.each do |plan| %>
      <% if plan.finished_product.name.include?('기정떡') %>
        <% log = plan.production_logs.first || ProductionLog.new %>
        <% if log.new_record? %>
          // 새 레코드인 경우에만 기본값 적용
          <% @gijeongddeok_fields.each do |field| %>
            const field_<%= plan.id %>_<%= field.field_name %> = document.querySelector(`#production_log_<%= plan.id %>_<%= field.field_name %>`);
            if (field_<%= plan.id %>_<%= field.field_name %> && field_<%= plan.id %>_<%= field.field_name %>.value === '') {
              field_<%= plan.id %>_<%= field.field_name %>.value = defaults.<%= field.field_name %> || '';
            }
          <% end %>
        <% end %>
      <% end %>
    <% end %>
  }

  // DOMContentLoaded와 turbo:load 모두에서 실행
  document.addEventListener('DOMContentLoaded', loadGijeongddeokDefaults);
  document.addEventListener('turbo:load', loadGijeongddeokDefaults);

  // 반죽 통수 +/- 버튼 핸들러
  function adjustValue(fieldId, delta) {
    const field = document.getElementById(fieldId);
    if (field) {
      const currentValue = parseFloat(field.value) || 0;
      const newValue = currentValue + delta;
      field.value = Math.max(0, newValue).toFixed(1);
    }
  }
</script>

<style>
  .nav-tab-modern {
    background: none;
    border: none;
    border-bottom: 3px solid transparent;
    color: #1d1d1f;
    font-weight: 500;
    padding: 0.75rem 1.5rem;
    transition: all 0.2s ease;
    cursor: pointer;
  }

  .nav-tab-modern:hover:not(.active) {
    color: #0071e3;
    border-bottom-color: #d2d2d7;
  }

  .nav-tab-modern.active {
    color: #0071e3;
    border-bottom-color: #0071e3;
  }

  .tab-content > .tab-pane {
    background-color: white;
  }
</style>
