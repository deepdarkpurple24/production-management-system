<%= render 'recipes/shared/sub_navigation' %>

<div class="container mt-4">
  <!-- 헤더 섹션 -->
  <div class="mb-3">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><%= link_to "레시피 관리", recipes_path %></li>
        <li class="breadcrumb-item active" aria-current="page">새 레시피 등록</li>
      </ol>
    </nav>
    <h4 class="mb-0" style="font-weight: 600; letter-spacing: -0.02em;">새 레시피 등록</h4>
    <p class="text-muted small mb-0">New Recipe</p>
  </div>

  <!-- 레시피 등록 폼 -->
  <div class="card-modern">
    <div class="card-body p-4">
      <%= render 'recipes/form', recipe: @recipe %>
    </div>
  </div>
</div>
